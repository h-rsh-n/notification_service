const serverConfig = require("./server-config");
const loggerConfig = require('./logger-config');
const mailerConfig = require('./email-config')
module.exports = {
  serverConfig,
  loggerConfig,
  mailerConfig
}